# user-service dependencies
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
# password hashing (backlog User N1.1) — uncomment when implementing auth
# passlib[bcrypt]>=1.7
# python-jose[cryptography]>=3.3   # JWT issue/verify
# messaging (publish UserRegistered) — uncomment when implementing events
# aio-pika>=9.4

# shared contracts library is installed separately (see Dockerfile / make install)
# because its path differs between local venv (../shared) and Docker (/shared).

# dev/test
pytest>=8.2
pytest-asyncio>=0.23
httpx>=0.27
