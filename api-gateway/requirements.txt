# api-gateway dependencies
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27          # reverse-proxy calls to backend services
# python-jose[cryptography]>=3.3   # JWT validation — uncomment when implementing

# shared contracts library is installed separately (see Dockerfile / make install)

# dev/test
pytest>=8.2
pytest-asyncio>=0.23
